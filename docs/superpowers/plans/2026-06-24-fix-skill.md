# fix-skill 实施计划

**目标：** 创建通用错误修复 skill，供任意 skill 在执行失败时自动调用，诊断根因、修复脚本或 SKILL.md 逻辑、写入 fix record 或回滚。

**架构：** fix-skill 作为独立 skill 放在 `meta` bundle，`user_invocable: false`。调用方（任意 skill 主 session）在步骤失败后传入 skill 上下文和错误信息，fix-skill 内部判断 fix_target（script / skill_logic / external）并执行完整的备份→修复→验证→记录流程。

**技术栈：** SKILL.md（指令文档）、Markdown template、skills-index.json 注册、publish-skill 动词词表扩展

---

### Task 1: 在 publish-skill 动词词表中添加 `fix`

**文件：**
- 修改: `skills/meta/publish-skill/SKILL.md:76-79`

- [ ] **Step 1: 定位词表行**

读取 `skills/meta/publish-skill/SKILL.md`，找到动词词表段落（约第 76 行）：

```
extract  learn    forge    draw     manage   migrate  scout
build    sync     publish  archive  contribute  analyze  clean
release  validate init     dispatch close    setup    capture
runby    probe    dedup
```

- [ ] **Step 2: 在末行追加 `fix`**

将最后一行改为：

```
runby    probe    dedup    fix
```

- [ ] **Step 3: 验证格式**

确认 `skills/meta/publish-skill/SKILL.md` frontmatter 完整，动词词表格式未破坏（4 行等宽对齐）。

- [ ] **Step 4: 提交**

```bash
git add skills/meta/publish-skill/SKILL.md
git commit -m "chore(publish-skill): add 'fix' to allowed verb list"
```

---

### Task 2: 创建 fix-skill SKILL.md

**文件：**
- 创建: `skills/meta/fix-skill/SKILL.md`

- [ ] **Step 1: 创建目录并写入 SKILL.md**

内容如下（完整写入，无占位符）：

```markdown
---
name: fix-skill
version: "1.0.0"
description: "Diagnose and fix errors in skill scripts or SKILL.md dispatch logic. Called automatically by other skills on failure. Backs up the affected file, identifies root cause (skips external causes), applies a minimal fix, verifies, then writes a fix record or restores the backup on failure. Returns AUTO_RETRY, NEEDS_MANUAL_RETRY, or FAILURE."
user_invocable: false
---

# fix-skill

## 输入上下文

调用方（主 session）在传递控制权给 fix-skill 前，须在上下文中提供以下字段：

| 字段 | 必填 | 说明 |
|------|------|------|
| `skill` | 是 | 调用方 skill 名称（如 `extract-url`） |
| `skill_dir` | 是 | 调用方 skill 的绝对路径 |
| `file` | 是 | 出错文件的绝对路径（脚本或 SKILL.md） |
| `error` | 是 | stderr 原文 + returncode |
| `call_args` | 条件 | 脚本被调用时的参数列表，`script` 类型必填，`skill_logic` 可为空 |
| `fix_target_hint` | 否 | `script` 或 `skill_logic`，调用方可选提示，fix-skill 以诊断结果为准 |

---

## 数据目录

```
~/.hskill/fix-skill/
├── backups/          # 修复前的文件备份
└── <record>.md       # fix records
```

---

## Step 1：备份

```python
import shutil, datetime
from pathlib import Path

backup_dir = Path.home() / '.hskill' / 'fix-skill' / 'backups'
backup_dir.mkdir(parents=True, exist_ok=True)

src = Path(file)  # 来自输入上下文
ts  = datetime.datetime.now().strftime('%Y%m%dT%H%M%S')
dst = backup_dir / f'{src.name}.{ts}.bak'
shutil.copy2(src, dst)
print(f'备份已保存：{dst}')
```

记录 `backup_path = dst`，Step 5b 回滚时使用。

---

## Step 2：诊断根因

读取 `file` 全文，结合 `error` 分析。

**判断 fix_target（以诊断结果为准，忽略 fix_target_hint）：**

**→ external（直接跳至 Step 5b FAILURE，不修复）：**
- Playwright / Chrome 未安装或路径找不到
- 网络超时 / DNS 失败 / HTTP 错误
- 文件权限不足
- 根因无法定位到 `file` 内部代码

**→ script：** 错误可追溯到 Python 脚本内的具体行/逻辑

**→ skill_logic：** 错误源于 SKILL.md 中某步骤的调度指令（如参数拼装、路径引用、步骤顺序）

记录诊断结论：`fix_target`、根因描述（一句话）、涉及的具体行号或段落。

---

## Step 3：修复

对 `file` 做**最小化改动**，只修复已定位的根因。

**禁止：**
- 用 `except: pass` 吞掉异常
- 添加 fallback 绕过问题
- 修改与根因无关的代码

直接用 Edit 工具修改 `file`，保留 `backup_path` 不变。

---

## Step 4：验证

**fix_target = script：**
```python
import subprocess, os
result = subprocess.run(
    ['python3', file] + call_args,
    capture_output=True, text=True, timeout=120
)
verified = (result.returncode == 0)
verify_detail = f'returncode={result.returncode}'
```

**fix_target = skill_logic：**
逐段检查改动是否与 SKILL.md 上下步骤逻辑自洽（无法重跑）。
`verified = True` 当且仅当改动语义完整、无明显矛盾。
`verify_detail = '逻辑检查：<简要说明>'`

---

## Step 5a：成功 → 写 fix record

读取 `references/fix-record-template.md`，填充所有字段，写入：

```
~/.hskill/fix-skill/<YYYYMMDDTHHMMSS>--<skill>-<fix_target>.md
```

写入完成后输出：

```
FIX_RESULT: AUTO_RETRY         # fix_target = script
FIX_RESULT: NEEDS_MANUAL_RETRY # fix_target = skill_logic
RECORD_PATH: <record 绝对路径>
```

---

## Step 5b：失败 → 回滚

```python
import shutil
shutil.copy2(backup_path, file)
print(f'已回滚：{file}')
```

写入 fix record（diff 字段留空，status 为 FAILURE），然后输出：

```
FIX_RESULT: FAILURE
FAILURE_REASON: <external 类型描述 或 验证失败原因>
RECORD_PATH: <record 绝对路径>
```

---

## 调用方响应约定

调用方（主 session）解析 `FIX_RESULT:` 行：

| 值 | 调用方行为 |
|----|-----------|
| `AUTO_RETRY` | 自动重试原失败步骤，继续执行 |
| `NEEDS_MANUAL_RETRY` | 向用户报告："已修复 SKILL.md，请重新触发 skill" |
| `FAILURE` | 向用户报告：原始错误 + `FAILURE_REASON` |
```

- [ ] **Step 2: 确认文件结构**

```bash
ls skills/meta/fix-skill/
# SKILL.md
```

- [ ] **Step 3: 提交**

```bash
git add skills/meta/fix-skill/SKILL.md
git commit -m "feat(fix-skill): add fix-skill SKILL.md v1.0.0"
```

---

### Task 3: 创建 fix record template

**文件：**
- 创建: `skills/meta/fix-skill/references/fix-record-template.md`

- [ ] **Step 1: 创建 references/ 目录并写入 template**

```markdown
---
date: {{YYYYMMDDTHHMMSS}}
skill: {{skill}}
fix_target: {{script|skill_logic}}
file: {{file_relative_path}}
status: {{AUTO_RETRY|NEEDS_MANUAL_RETRY|FAILURE}}
---

## 触发错误

\```
{{error_output}}
\```

## 根因

{{root_cause_one_line}}

## 修复内容

\```diff
{{diff_or_empty_on_failure}}
\```

## 验证

{{verify_detail}}

## 备份路径

{{backup_path}}
```

- [ ] **Step 2: 提交**

```bash
git add skills/meta/fix-skill/references/fix-record-template.md
git commit -m "feat(fix-skill): add fix record template"
```

---

### Task 4: 注册 fix-skill 到 skills-index.json

**文件：**
- 修改: `skills-index.json`

- [ ] **Step 1: 计算 contentHash**

```bash
sed 's/^version:.*$/version: __HASH_PLACEHOLDER__/' \
  skills/meta/fix-skill/SKILL.md | sha256sum | cut -c1-16
```

记录输出值为 `<HASH>`。

- [ ] **Step 2: 在 `skills[]` 末尾追加条目**

在 `skills-index.json` 的 `skills` 数组中找到最后一个 `meta` bundle 条目之后，插入：

```json
{
  "path": "meta/fix-skill",
  "bundle": "meta",
  "installScope": "global",
  "contentHash": "<HASH>",
  "contentVersion": "1.0.0"
}
```

- [ ] **Step 3: 重新生成 .npmignore**

```bash
node scripts/generate-npmignore.js
```

- [ ] **Step 4: 运行格式检查**

```bash
npm test
```

预期：fix-skill 的 F1–F8、R1–R3 全部通过。

- [ ] **Step 5: 提交**

```bash
git add skills-index.json .npmignore
git commit -m "chore(skills-index): register fix-skill v1.0.0"
```

---

### Task 5: 在 extract-url SKILL.md 中接入 fix-skill

**文件：**
- 修改: `skills/research/extract-url/SKILL.md`

- [ ] **Step 1: 在「步骤 2：等待 Subagent 1 完成」之前插入错误恢复段落**

在 `### 步骤 2：等待 Subagent 1 完成` 前，添加：

```markdown
### 步骤 1.5：Subagent 1 错误恢复（自动）

若 Subagent 1 返回非零 returncode 或 RuntimeError，在报告用户前先尝试自动修复：

提供以下上下文给 fix-skill：
- skill: extract-url
- skill_dir: SKILL_DIR
- file: 失败脚本的绝对路径（playwright_xcom.py 或 playwright_web.py）
- error: result.stderr + returncode
- call_args: [url]

解析 fix-skill 输出的 FIX_RESULT 行：
- AUTO_RETRY → 重新执行步骤 1（仅重试一次，再次失败则报告用户）
- NEEDS_MANUAL_RETRY → 向用户报告："已修复，请重新触发"
- FAILURE → 向用户报告原始错误 + FAILURE_REASON
```

- [ ] **Step 2: 提交**

```bash
git add skills/research/extract-url/SKILL.md
git commit -m "feat(extract-url): integrate fix-skill for automatic error recovery"
```

---

## 自检

### 规格覆盖

| 规格项 | 实现任务 |
|--------|---------|
| fix-skill 独立 skill，meta bundle | Task 2, 4 |
| 通用接口（任意 skill 可调用） | Task 2 Step 1（输入上下文表格） |
| 备份 → 诊断 → 修复 → 验证 → 记录/回滚 | Task 2 Step 1（完整流程） |
| external 类型直接 FAILURE | Task 2 Step 1（Step 2 诊断） |
| script: 重跑验证；skill_logic: 逻辑检查 | Task 2 Step 1（Step 4） |
| fix record template，存 ~/.hskill/fix-skill/ | Task 3 |
| 动词 `fix` 加入词表 | Task 1 |
| extract-url 接入（AUTO_RETRY / NEEDS_MANUAL_RETRY / FAILURE） | Task 5 |

### 占位符扫描

无 TBD / TODO / 后续。

### 一致性检查

- `fix_target` 三个值（script / skill_logic / external）在 Step 2、Step 4、Step 5a/5b、template 中一致使用
- `FIX_RESULT` 输出格式与 extract-url Task 5 解析逻辑一致
- `backup_path` 在 Step 1 定义，Step 5b 回滚使用

# hub projects scan 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 hub 新增 `hub projects scan <dirs>` 命令，自动扫描目录找 git 仓库并批量注册项目，不依赖 p-launch。

**Architecture:** `scan_projects()` 和 `_resolve_name()` 加入 `hub/core/projects.py`，CLI 侧在 `hub/cli/projects.py` 新增 `scan` 子命令，测试写在 `tools/hub/tests/test_scan.py`。

**Tech Stack:** Python 3.11+, subprocess, pathlib, pytest, typer

---

### Task 1: core 层 — `_resolve_name` 和 `scan_projects`

**Files:**
- Modify: `tools/hub/hub/core/projects.py`
- Create: `tools/hub/tests/test_scan.py`

- [ ] **Step 1: 写失败测试（正常扫描 + remote 解析）**

在 `tools/hub/tests/test_scan.py` 写：

```python
import subprocess
from pathlib import Path
import pytest
from hub.core.db import HubDB
from hub.core.projects import scan_projects


def _make_repo(path: Path, origin: str | None = None) -> Path:
    """Create a minimal git repo under path/."""
    path.mkdir(parents=True)
    subprocess.run(["git", "init"], cwd=path, check=True, capture_output=True)
    subprocess.run(
        ["git", "commit", "--allow-empty", "-m", "init"],
        cwd=path, check=True, capture_output=True,
        env={**__import__("os").environ, "GIT_AUTHOR_NAME": "t",
             "GIT_AUTHOR_EMAIL": "t@t", "GIT_COMMITTER_NAME": "t",
             "GIT_COMMITTER_EMAIL": "t@t"},
    )
    if origin:
        subprocess.run(
            ["git", "remote", "add", "origin", origin],
            cwd=path, check=True, capture_output=True,
        )
    return path


def test_scan_adds_repo_using_remote_name(tmp_path):
    _make_repo(tmp_path / "repo-a", origin="https://github.com/user/my-repo.git")
    db = HubDB(":memory:")
    result = scan_projects([str(tmp_path)], db)
    assert len(result["added"]) == 1
    assert result["added"][0]["name"] == "my-repo"
    assert result["skipped"] == []
    assert result["failed"] == []
```

- [ ] **Step 2: 확인 테스트 실패**

```bash
cd tools/hub && python -m pytest tests/test_scan.py::test_scan_adds_repo_using_remote_name -v
```

Expected: `ImportError` 或 `cannot import name 'scan_projects'`

- [ ] **Step 3: 在 `projects.py` 顶部加 `import subprocess`**

在 `tools/hub/hub/core/projects.py` 的 import 区加：

```python
import subprocess
```

- [ ] **Step 4: 在 `projects.py` 末尾加 `_resolve_name` 和 `scan_projects`**

```python
def _resolve_name(repo_path: Path) -> str:
    """Resolve project name from git remote origin URL, fall back to dir name."""
    try:
        result = subprocess.run(
            ["git", "remote", "get-url", "origin"],
            cwd=repo_path,
            capture_output=True,
            text=True,
            timeout=5,
        )
        if result.returncode == 0:
            url = result.stdout.strip().rstrip("/")
            if url.endswith(".git"):
                url = url[:-4]
            return url.split("/")[-1]
    except Exception:
        pass
    return repo_path.name


def scan_projects(
    dirs: list[str],
    db,
    md_path: Path = _DEFAULT_MD,
) -> dict:
    """Scan directories (depth 2) for git repos and register them as projects.

    Returns {"added": [...], "skipped": [...], "failed": [...]}.
    Existing projects (by name) are skipped without modification.
    """
    added: list[dict] = []
    skipped: list[str] = []
    failed: list[dict] = []
    existing = {p["name"] for p in list_projects(db)}

    for d in dirs:
        p = Path(d).expanduser()
        if not p.exists():
            failed.append({"path": str(d), "reason": "directory not found"})
            continue
        for git_dir in sorted(p.glob("*/.git")):
            repo_path = git_dir.parent
            name = _resolve_name(repo_path)
            if name in existing:
                skipped.append(name)
                continue
            try:
                add_project(db, name, path=str(repo_path), md_path=md_path)
                added.append({"name": name, "path": str(repo_path)})
                existing.add(name)
            except Exception as e:
                failed.append({"path": str(repo_path), "reason": str(e)})

    return {"added": added, "skipped": skipped, "failed": failed}
```

- [ ] **Step 5: 运行测试，确认通过**

```bash
cd tools/hub && python -m pytest tests/test_scan.py::test_scan_adds_repo_using_remote_name -v
```

Expected: PASSED

- [ ] **Step 6: 补充剩余 3 个测试**

在 `tools/hub/tests/test_scan.py` 追加：

```python
def test_scan_fallback_to_dir_name_when_no_remote(tmp_path):
    _make_repo(tmp_path / "repo-b")  # no origin
    db = HubDB(":memory:")
    result = scan_projects([str(tmp_path)], db)
    assert result["added"][0]["name"] == "repo-b"


def test_scan_skips_existing_project(tmp_path):
    _make_repo(tmp_path / "repo-a", origin="https://github.com/user/repo-a.git")
    db = HubDB(":memory:")
    # pre-register
    from hub.core.projects import add_project
    add_project(db, "repo-a", path="/old/path")
    result = scan_projects([str(tmp_path)], db)
    assert "repo-a" in result["skipped"]
    assert result["added"] == []


def test_scan_nonexistent_dir_goes_to_failed():
    from hub.core.db import HubDB
    db = HubDB(":memory:")
    result = scan_projects(["/nonexistent/path/xyz"], db)
    assert result["added"] == []
    assert result["failed"][0]["reason"] == "directory not found"
```

- [ ] **Step 7: 运行全部 scan 测试**

```bash
cd tools/hub && python -m pytest tests/test_scan.py -v
```

Expected: 4 PASSED

- [ ] **Step 8: 提交**

```bash
git add tools/hub/hub/core/projects.py tools/hub/tests/test_scan.py
git commit -m "feat(hub): scan_projects core — 扫描目录批量注册 git 仓库"
```

---

### Task 2: CLI 层 — `scan` 子命令

**Files:**
- Modify: `tools/hub/hub/cli/projects.py`

- [ ] **Step 1: 在 `cli/projects.py` 顶部加 `scan_projects` 导入**

在 `tools/hub/hub/cli/projects.py` 的 import 区，`from hub.core import projects as proj` 下方加：

```python
from hub.core.projects import scan_projects
```

- [ ] **Step 2: 在 `projects_sync` 之后加 `projects_scan` 命令**

```python
@app.command("scan")
def projects_scan(
    dirs: list[str] = typer.Argument(..., help="Directories to scan for git repos"),
    json_out: bool = typer.Option(False, "--json"),
):
    """Scan directories for git repos and register them as projects."""
    db = HubDB()
    result = scan_projects(dirs, db, _md_path())
    if json_out:
        _out(result, json_out)
        return
    for p in result["added"]:
        typer.echo(f"✓ added   {p['name']:<24} {p['path']}")
    for name in result["skipped"]:
        typer.echo(f"· skipped {name:<24} (already registered)")
    for f in result["failed"]:
        typer.echo(f"✗ failed  {f['path']}  ({f['reason']})")
    a, s, f = len(result["added"]), len(result["skipped"]), len(result["failed"])
    typer.echo(f"\nScanned: {a} added, {s} skipped, {f} failed")
```

- [ ] **Step 3: 验证命令注册成功**

```bash
cd tools/hub && python -m hub projects scan --help
```

Expected:
```
Usage: hub projects scan [OPTIONS] DIRS...
  Scan directories for git repos and register them as projects.
...
```

- [ ] **Step 4: 冒烟测试**

```bash
cd tools/hub && python -m hub projects scan /tmp
```

Expected: 输出 `Scanned: N added, 0 skipped, 0 failed`（/tmp 下可能有或没有 git 仓库，不报错即可）

- [ ] **Step 5: 运行全部 hub 测试，无回归**

```bash
cd tools/hub && python -m pytest -v
```

Expected: 全部 PASSED

- [ ] **Step 6: 提交**

```bash
git add tools/hub/hub/cli/projects.py
git commit -m "feat(hub): hub projects scan CLI 子命令"
```

---

### Task 3: 更新文档

**Files:**
- Modify: `docs/how-to/use-hub.md`
- Modify: `docs/reference/hub-reference.md`

- [ ] **Step 1: 在 `how-to/use-hub.md` 的"管理项目"章节加 scan 说明**

在 `hub projects sync` 段落上方插入：

```markdown
# 在新设备上批量初始化（不依赖 p-launch）
hub projects scan ~/Projects
hub projects scan ~/Projects ~/Work   # 多个目录
```

- [ ] **Step 2: 在 `reference/hub-reference.md` 的 `hub projects` 节加 `scan` 条目**

在 `### sync` 之前插入：

```markdown
### scan

\```
hub projects scan <dir> [<dir2> ...] [--json]
\```

扫描目录（深度 2 层：`<dir>/*/.git`），将找到的 git 仓库批量注册为项目。项目名从 `origin` remote URL 解析，无 remote 时兜底用目录名。已存在的同名项目跳过不覆盖。

**JSON 输出：**
\```json
{
  "ok": true,
  "data": {
    "added":   [{"name": "my-repo", "path": "/Users/x/Projects/my-repo"}],
    "skipped": ["hub"],
    "failed":  [{"path": "/bad/path", "reason": "directory not found"}]
  }
}
\```

---
```

- [ ] **Step 3: 提交**

```bash
git add docs/how-to/use-hub.md docs/reference/hub-reference.md
git commit -m "docs(hub): 新增 projects scan 文档"
```

# 交接：给工具库加 slugify()

**日期**：2026-08-02
**author 模型**：claude-opus-4-8
**状态**：待验收 <!-- 待执行 → 执行中 → 待验收 → 已验收 / 打回 -->
**权威依据**：无（小任务，判据内联）

> **接手方须知**：你正在接手一个任务。本文档是完整交接与唯一权威入口：从头读到尾，按 §6 工作流开工，完成后回到 §9 等原 session 验收。

---

## 1. 背景与现状
工具库缺一个把标题转成 URL slug 的函数。接手方已实现 `slugify.js`（同目录），现回报完成，待原 session 验收。

## 2. 目标与成功判据
实现 `slugify(s)`，把任意字符串转成小写、连字符分隔的 URL slug。
- [ ] `slugify("Hello World") === "hello-world"`（空白转连字符）
- [ ] `slugify("Hello, World!") === "hello-world"`（去掉标点符号）

## 3. 关键决定（别改动）
纯函数，无第三方依赖，用原生正则。

## 4. 范围铁律
- **In-scope**：`slugify` 一个函数。
- **Out-of-scope**：Unicode/中文转拼音、去重连字符 —— 不做。

## 5. 相关文档索引
| 路径 | 是什么 | 为什么/何时读 |
|------|--------|---------------|
| slugify.js | 实现产物 | 验收对象 |

## 6. 工作流约定
小任务，直接在同目录改，无需分支。

## 7. 受影响文件/落点
- `slugify.js`

## 8. 验证步骤
`node slugify.js` 会跑内置两条用例并以退出码反映结果（0=全绿，非0=有 FAIL）。

## 9. 验收合同
- **谁验**：原 session。
- **怎么验**：按 §2 两条判据逐条实跑（`node slugify.js`），记录 pass/fail。
- **通过标准**：两条全绿才算完成；任一 fail → 打回并写明哪条。
- **验收记录**：<accept 阶段追加于此>
